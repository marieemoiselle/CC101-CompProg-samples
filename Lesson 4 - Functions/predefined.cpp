# include <iostream>
# include <cmath>
using namespace std;

int main () {
    double value = pow(7, 2);
    double sqr_root = sqrt(64);
    cout << "Value: " << value << endl;
    cout << "Square root of 64: " << sqr_root;
    return 0;
}